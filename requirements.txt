# Desk-sorting arm dependencies.
#
# Python 3.10+ is REQUIRED: build123d declares requires-python ">=3.10,<3.15".
# The CI matrix (.github/workflows/tests.yml) therefore covers 3.10-3.13.

# ---- Phase A: kinematics simulation --------------------------------------
numpy>=1.24,<3.0
scipy>=1.10,<2.0
matplotlib>=3.7,<4.0
ikpy>=3.4,<5.0
pytest>=7.0,<10.0

# ---- Phase B: computer vision --------------------------------------------
# The *contrib* build is required: cv2.aruco lives in opencv_contrib, not core.
# Pinned below 5.0 deliberately - OpenCV 5.x is a major release and the ArUco
# API surface has not been validated against it here.
opencv-contrib-python>=4.8,<5.0

# ---- Phase D: parametric CAD ---------------------------------------------
# Pulls in cadquery-ocp (OpenCASCADE bindings); the wheel is ~300 MB.
build123d>=0.9,<0.12
